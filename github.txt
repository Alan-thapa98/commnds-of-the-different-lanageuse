lh 
pwd 
ls
clear
git config --global user.name Alan thapa
git config --global user.email alanthapa98.gmail.com
git config --global user.name --to chake the info 

code.

git init
ls -lart #show the hidden files
git add  fname and tab to autocomplete
git status
git commit 
Initial commit #fill it 
:wq
git status
git add -A add the the upade of all files
git add -A and name (of the file to update an individual file)
git status
git status -s 
git commit -m " mss "
git commit -a -m " mss all is commit skipped the commit to all files"

 
# reomove the file
git rm name.html #delete from both git and disk
git rm --cached 

# it will shwothe particular change with the last one 
 git diff 
 git diff --staged 3 COMPAIR SHOW THE LAST COMMIT 

# it will show the commint files that you change it 
git log
git log -p -2


# these create blank file 
 touch about.html to create file
# touch .gitignore

# emergancy if you maitakey update the file it will reover the last commit file remmember only the commit file 
make sure you commit the file if only you are sure that you complteted the your work properly 
okay!
git checkout -f 
git checkout nameofthefile


# branch 
git branch 
1.git checkout master # main banch 
2.git checkout feature1
2.git checkout -b django # add the branch plus switch into django 
3.git commit -a -m " mss all is commit skipped the commit to all files"
4. git merge feature1 # it wil merge both master and feature1 files
5.git log -p -2 



# push the repo
1. git remote add origin https://github.com/Alan-thapa98/git-test.git
2. git remote
3. git remote -v 
4. git push origin master 
5. ssh-keygen -t ed25519 -C "test.com"
6. ssh-keygen -t rsa -b 4096 -C "testweb.com"
6. eval "$(ssh-agent -s)"
7. ssh-add ~/.ssh/id_ed25519
7. cat ~/.ssh/id_ed25519





git remote add nicename (addlinetheofrepo)







git init
git status
git add .
git status
git commit -m "test dark 
git remote add origin git@github.com:Alan-thapa98/my.git"
git push -u origin master


ssh-keygen -t rsa -b 4096 -C "alanthapa98@gmail.com"





ssh-keygen
